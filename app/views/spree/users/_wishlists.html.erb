<h3 class="account-summary-title"><%= Spree.t(:my_wishlists) %></h3>
<ul class="list-unstyled account-summary">
<% @user.wishlists.each do |wl| %>
  <% if !wl.is_private? || @user == spree_current_user %>
  <li><%= link_to wl.name, wl %></li>
  <% end -%>
<% end -%>
</ul>